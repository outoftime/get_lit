= render :partial => 'types', :object => %w(beer highball wine teapot)

.main
  #logo
  .content#home
    %h1 What are you reading?
    %form(action="search")
      %input.title-input{:type => "text", :name => "title", :placeholder => "Book title"}
      %button.recommend{:type => "submit"} What should I drink?

= render :partial => 'types', :object => %w(cocktail shots testtube lowball)
